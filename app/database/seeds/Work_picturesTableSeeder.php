<?php

class Work_picturesTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('work_pictures')->truncate();

		$work_pictures = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('work_pictures')->insert($work_pictures);
	}

}
